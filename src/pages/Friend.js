import NavBar from "../components/NavBar";

const Friend = () => {
    return (
        <div className="container">
            <h1>Friend</h1>
            <NavBar />
        </div>
    );
};

export default Friend;
